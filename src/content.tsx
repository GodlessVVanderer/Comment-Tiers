console.log("YouTube Comment Analyzer content script loaded.");

// This script can be used to inject UI elements into the YouTube page,
// for example, adding an "Analyze Comments" button.
// For simplicity, we'll keep this file minimal as the main UI is in the popup.
export {};
